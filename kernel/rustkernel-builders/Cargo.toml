[package]
name = "rustkernel-builders"
version = "0.1.0"
edition = "2021"

[dependencies]
rustkernel-math = { path = "../rustkernel-math" }
rustkernel-topology = { path = "../rustkernel-topology" }
rustkernel-geom = { path = "../rustkernel-geom" }
curvo = "0.1"
tracing = "0.1"
